# Gogi landing page

Static HTML/CSS/JS landing page for Gogi. No build step or dependencies are required.

## Local preview

Open `index.html` in a browser or serve this directory with any static file server.

## Deployment

The project is configured for Vercel static deployment. The original Gogi GIFs live in `assets/` and are intentionally kept as local files.
